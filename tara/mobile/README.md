# Tara Mobile

Flutter app (Android only for now).

## Running

```bash
cd tara/mobile
flutter run
```

By default the app calls the API at `http://10.0.2.2:8000` (Android emulator
loopback). Update `lib/state/analysis_state.dart` if you need a different URL.
